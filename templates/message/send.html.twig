{% extends 'base.html.twig' %}

{% block title %}Contacter un autre utilisateur !{% endblock %}

{% block body %}
<main class="message">
    <div class="message__form">
        <h1 class="message__form__title">Ajouter une annonces</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Valider" class="message__form__button">
        {{ form_end(form) }}
    </div>
    <form action="{{ path('message_ping') }}" method="post">
        <button>ping !</button>
    </form>
    <form action="{{ path('message_ping', {user: app.user.profile.id}) }}" method="post">
        <button>ping !</button>
    </form>
</main>
</main>
{% endblock %}