{% extends 'base.html.twig' %}

{% block title %}Ajout information profil{% endblock %}

{% block body %}
    <main class="profile">
        <div class="profile__form">
            <h1 class="profile__form__title">compléter votre profile</h1>
            {{ form_start(form) }}
            {# {{ form_widget(form) }} #}
            {{ form_row(form.gender) }}
            {{ form_row(form.imageFile) }}
            {{ form_row(form.username) }}
            {{ form_row(form.firstname) }}
            {{ form_row(form.lastname) }}
            {{ form_row(form.birthday) }}
            {{ form_row(form.number) }}
            {{ form_row(form.address) }}
            {{ form_row(form.propose) }}
            {{ form_row(form.research) }}
            <input type="submit" value="Valider" class="profile__form__button">
            {{ form_end(form) }}
        </div>
    </main>
{% endblock %}

